# Level metadata for the UNHCR Vessel.
# Source-of-truth tilemap defines walls; all other assets are visual.

version = "1"
name = "UNHCR Vessel"

# Tilemap (required): physics tile grid, CSV with int values per cell
# Values: 0=outer space (vacuum), 1=hull wall, 3=door, 2/4/5/6/7/8=interior air
tilemap = "tilemap.csv"

# Physical scale
tile_size_m = 0.333    # 1/3 meter per tile

# Art layers
diffuse = "diffuse.png"           # required: base color art
normal = "normal.png"             # optional: surface normals for lighting
# emissive_mask = "emissive_mask.png"  # optional: pixels that glow (omitted)
# emissive_bloom = "emissive_bloom.png" # optional: blurred halos     (omitted)
# wall_mask = "wall_mask.png"           # optional: overrides CSV-derived walls (omitted)

# Multi-floor support (v2 — reserved schema)
floor_id = 0
# teleporters = [
#     { from = [tile_x, tile_y], to_level = "next_level", to_tile = [x, y] }
# ]
