# S8c kickoff — render/recorder device residency + the `cast_fire_heat` port

> Ready-to-paste session prompt (written by Fable at the S8a Path-A close,
> 2026-07-21, while the resident-tick internals were fresh). Paste the block
> below into a fresh session. Erik picks the model; the workflow is
> autonomous-patch-workflow with a DESIGN PASS FIRST for the fire-heat port
> (it is sim-affecting); the render/recorder pieces are render-layer
> (determinism-EXEMPT) and may run as ordinary gated patches.

---

You are closing **S8c** on the breach project: the last S8 residency tier —
make the *consumers* of the physics fields stop paying per-tick transfer
taxes. Physics engine v1 is CLOSED (S8a complete, merge `93a014c`; the ledger
records the arc). Three work items, in priority order:

**1. The `cast_fire_heat` device port — the fire-FPS fix (sim-affecting,
design-pass first).** Today it is a host Python loop in
`src/simulation/physics_runner.py::cast_fire_heat` (~line 790): every burning
tile becomes a `LightSource` and is cast ONE SOURCE PER CALL through the
raycaster — on `--cuda` each call uploads the running `heat` plane, marches,
and downloads it back (`bp.cuda_raycaster_cast`). With hundreds of burning
tiles that is hundreds of round-trips per tick — Erik measured 3 fps
(2026-07-20 B5 feel-test; NOT a BC regression). The port: build the source
list host-side (row-major over `fire > 0`, the existing deterministic phase
formula `(x*7+y*13) % ray_count`), then ONE batched device cast for ALL
sources into a device `heat` plane. Determinism notes from the S8a work:
`heat` deposits are saturating atomic adds (order-free — the proven CUDA-S2
argument) but VERIFY the per-source march arithmetic stays byte-identical to
the per-call path (the existing raycaster digest gate is the oracle;
`gmap.heat` is a synced field compared tol-0 in `tests/cuda_s8a_check.py`
PART 1). In the resident tick, `cast_fire_heat` runs in host pre-physics
(step 1) BEFORE the EOS pre-upload — a device-resident cast must either land
`heat` back on the mirror (simplest, keeps the tick contract) or move the
consumer reads (tail temperature pass + unit damage) device-side (bigger —
probably out of scope). There is also an unused C++ `coarse_cluster` helper
the 2026-07-20 notes flag as relevant. The per-call GPU path and CPU path
stay live and byte-identical; no re-baseline.

**2. Render CUDA-GL interop.** The renderer reads the numpy mirror; on
`--cuda --resident` several render inputs now live on device and round-trip
through the mirror only for display. Interop (CUDA-GL shared buffers) lets
render-only fields skip the host. Render-layer = determinism-EXEMPT (iron
rules), but the SYNCED fields' mirror contract (the locked Q4 decision) must
not change: combat/recorder still read the mirror. Scope to what render
actually reads; do not drop the Q4 mirror (that is the batched-training
end-state, not S8c).

**3. Recorder kernels.** The recorder snapshots synced fields per tick from
the mirror. Device-side snapshot/compaction kernels can cut its cost; the
recorded BYTES must stay identical (recordings are replay/desync evidence).

**Environment (Lenovo/Ada):** python =
`C:/Users/steen/miniconda3/envs/data/python.exe` (NOT `conda run` — plugin
crash). Builds: `cpp/build_cuda_lenovo.bat` + `cpp/build_cpu_data.bat`.
⚠ After any merge, REBUILD the main tree's `.pyd`s before running `main.py`.
Worktree per the concurrent-agent rules; commit the design doc to the branch
before spawning worktree agents.

**Gates:** `cast_fire_heat` port → the existing raycaster digest gate + the
S8a full-engine A/B (`tests/cuda_s8a_check.py`, `heat` is in `_FIELDS`,
tol 0) + a PART-2-style payoff bench (a many-fires scenario: per-call cast vs
batched resident cast; the 3-fps scenario must become playable). Render
interop + recorder: recorded-bytes identity + full `pytest tests -q` green +
a HUMAN-TEST feel check (render changes are Erik-gated — feel-adjacent
changes never auto-merge). Mechanical digest-gated pieces may auto-merge on
green ONLY if Erik pre-authorizes in the session.

**Fresh S8a facts you inherit** (design doc
`docs/cuda_s8a_path_a_impl_2026-07-21.md`): the resident tick is
`_step_resident` — water resident → EOS fully resident (one pre-upload,
zero mid-tick transfers) → traces resident → one synced-set D2H → combustion
+ tail brackets (S8c may now attack those brackets' consumers). ★
`is_ambient` is NOT static (joins-ambient breach) — check structural-edit
writers before treating any mask as static. The resident path skips the six
`digest_*` checkpoints (documented gap). Laptop bench ratios are power-state
noisy — assert wins + absolute floors, not ratio ordering.

**NOT this task:** S8b CUDA graphs (parked — its payoff is many-env
training; needs a design pass on the n_sub-variable launch topology).
Dropping the Q4 mirror. Batched training.
