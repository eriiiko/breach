# report_t6.md — T6: delete the old heat law (issue #12)

> Branch `12-t6-old-law-deletion` off `fire-12` at `3b9fda3`. Worktree
> `C:\Users\steen\projects\breach-t6`. Written incrementally; skeleton
> committed before any code change.

**Baseline**, this worktree, fresh CPU+CUDA build: `3420 passed, 4 skipped,
8 xfailed, 0 failed` in 263.73 s (`pytest tests -q -p no:cacheprovider`).
Timing probes before any edit (default_scenario_sim, radiatively inert:
0.83 ms/tick; playground + one hot burning wood tile, the old cast's real
worst case: 13.02 ms/tick).

## 1. Scope found by investigation

(filled incrementally as each area is confirmed)

## 2. What was deleted

### Python

### C++

### Bindings

### Config

### Tools

### Tests deleted (property — why it no longer exists)

## 3. What was kept, and why

## 4. Deviations from the stale design v3 P4 assignment

## 5. Gate

## 6. Timing before/after

## 7. Findings (not fixed — out of scope)

## For Erik
